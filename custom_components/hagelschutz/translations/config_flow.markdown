Gib die `device_id` ein, die du vom NetIT-Service erhalten hast.

Beispielhafte URL, die zur Datenabfrage verwendet wird:

```
https://meteo.netitservices.com/api/v0/devices/<device_id>/poll?hwtypeId=188
```